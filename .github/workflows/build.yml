name: Build Flock-Detector Unsigned IPA
- name: Install XcodeGen
      run: brew install xcodegen

      - name: Generate Xcode Project
            run: xcodegen generate

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-14

    env:
      SCHEME: "Flock-Detector"
      PROJECT: "Flock-Detector.xcodeproj"

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Select Xcode version
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Show available schemes (debug helper)
        run: |
          xcodebuild -list -project "$PROJECT" || true

      - name: Build unsigned archive
        run: |
          xcodebuild archive \
            -project "$PROJECT" \
            -scheme "$SCHEME" \
            -archivePath build/App.xcarchive \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            AD_HOC_CODE_SIGNING_ALLOWED=YES

      - name: Package unsigned IPA
        run: |
          set -e
          APP_PATH=$(find build/App.xcarchive/Products/Applications -maxdepth 1 -name "*.app" | head -n 1)
          echo "Found a
